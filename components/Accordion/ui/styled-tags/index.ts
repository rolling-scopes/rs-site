import { StyledAnchor } from './StyledAnchor';
import { StyledHeading } from './StyledHeading';

export const styledTags = {
  a: StyledAnchor,
  h1: StyledHeading,
  h2: StyledHeading,
  h3: StyledHeading,
  h4: StyledHeading,
  h5: StyledHeading,
  h6: StyledHeading
};
